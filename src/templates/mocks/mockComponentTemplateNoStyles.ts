export default `import React from 'react'
import { TestComponentProps } from './types'

const TestComponent = ({}: TestComponentProps): JSX.Element => {
    return <div>TestComponent says hi!</div>
}

export default TestComponent
`
